# Demo overlay. Web currently ships the larger 512x4 policy that actually plays.
# 3*H*H*L = 3145728 (over the 1M cap). 512x1 web1m retrain is still pending.

[env]
# One policy agent; NPCs/world sizes stay on nmmo3.ini
num_agents = 1

[policy]
hidden_size = 512
num_layers = 4
