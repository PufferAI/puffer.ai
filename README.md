# puffer.ai
Our website

Pretrained eval checkpoints: `docs/assets/models/`
`https://puffer.ai/assets/models/<name>_weights.bin`

Ocean thumbnails: `screenshot.c` (compile per env from the puffer repo; see the comment at the top of that file).
